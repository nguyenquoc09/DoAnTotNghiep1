@extends('layouts.doctor') @section('title','Lịch khám hôm nay') @section('pageTitle','Chào bác sĩ '.auth()->user()->name) @section('content')<div class="row g-3 mb-4"><div class="col-md-4"><div class="stat-card"><small>Đang chờ</small><strong>{{ $tickets->where('status','waiting')->count() }}</strong></div></div><div class="col-md-4"><div class="stat-card"><small>Đang khám</small><strong>{{ $tickets->where('status','in_progress')->count() }}</strong></div></div><div class="col-md-4"><div class="stat-card"><small>Đã hoàn thành</small><strong>{{ $tickets->where('status','completed')->count() }}</strong></div></div></div><div class="panel" id="queue"><div class="panel-title"><h2>Hàng đợi hôm nay</h2><span class="badge-status active">{{ now()->format('d/m/Y') }}</span></div><div class="table-responsive"><table class="table"><thead><tr><th>STT</th><th>Bệnh nhân</th><th>Thời gian tiếp nhận</th><th>Trạng thái</th><th></th></tr></thead><tbody>@forelse($tickets as $ticket)<tr><td><span class="avatar">{{ $ticket->queue_number }}</span></td><td><strong>{{ $ticket->patient->full_name }}</strong><small class="d-block text-muted">{{ $ticket->patient->patient_code }}</small></td><td>{{ optional($ticket->check_in_at)->format('H:i') }}</td><td><span class="badge-status {{ $ticket->status }}">{{ $ticket->status }}</span></td><td><a class="btn btn-sm {{ $ticket->status==='waiting' ? 'btn-primary' : 'btn-soft' }}" href="{{ route('doctor.examinations.show',$ticket) }}">{{ $ticket->status==='waiting' ? 'Tiếp nhận khám' : 'Mở hồ sơ' }}</a></td></tr>@empty<tr><td colspan="5" class="empty-state"><i class="bi bi-cup-hot"></i><p>Chưa có bệnh nhân trong hàng đợi.</p></td></tr>@endforelse</tbody></table></div></div>@endsection
