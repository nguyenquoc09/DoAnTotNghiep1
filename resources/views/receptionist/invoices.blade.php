@extends('layouts.receptionist') @section('title','Hóa đơn') @section('pageTitle','Hóa đơn & thanh toán') @section('content')<div class="panel mb-4"><details><summary class="btn btn-primary">Lập hóa đơn sau khám</summary><form method="post" action="{{ route('receptionist.invoices.store') }}" class="row g-3 mt-2">@csrf<div class="col-md-7"><label class="form-label">Phiên khám đã hoàn thành</label><select class="form-select" name="medical_record_id">@foreach($records as $r)<option value="{{ $r->id }}">{{ $r->record_code }} · {{ $r->patient->full_name }} · {{ $r->created_at->format('d/m/Y') }}</option>@endforeach</select></div><div class="col-md-3"><label class="form-label">Giảm giá</label><input class="form-control" type="number" name="discount_amount" value="0" min="0"></div><div class="col-md-2 d-flex align-items-end"><button class="btn btn-primary w-100">Lập hóa đơn</button></div></form></details></div><div class="panel"><form class="toolbar"><input class="form-control" name="q" value="{{ request('q') }}" placeholder="Mã hóa đơn, bệnh nhân"><select class="form-select" name="status"><option value="">Mọi trạng thái</option><option value="unpaid">Chưa thanh toán</option><option value="partially_paid">Thanh toán một phần</option><option value="paid">Đã thanh toán</option></select><button class="btn btn-primary">Lọc</button></form><div class="table-responsive"><table class="table"><thead><tr><th>Hóa đơn</th><th>Bệnh nhân</th><th>Tổng tiền</th><th>Đã thu</th><th>Trạng thái</th><th></th></tr></thead><tbody>@foreach($invoices as $i)<tr><td><strong>{{ $i->invoice_code }}</strong><small class="d-block text-muted">{{ $i->created_at->format('d/m/Y H:i') }}</small></td><td>{{ $i->patient->full_name }}</td><td>{{ number_format($i->total_amount) }}đ</td><td>{{ number_format($i->paid_amount) }}đ</td><td><span class="badge-status {{ $i->payment_status }}">{{ $i->payment_status }}</span></td><td><a class="btn btn-sm btn-soft" href="{{ route('receptionist.invoices.show',$i) }}">Chi tiết</a></td></tr>@endforeach</tbody></table></div>{{ $invoices->links() }}</div>@endsection
