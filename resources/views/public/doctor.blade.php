@extends('layouts.guest') @section('title',$doctor->user->name) @section('content')<section class="section section-soft"><div class="container"><div class="panel p-4 p-lg-5"><div class="row g-4"><div class="col-lg-4 text-center"><div class="doctor-avatar mx-auto" style="width:140px;height:140px;font-size:4rem"><i class="bi bi-person-heart"></i></div><span class="badge-status active mt-3">Đang nhận lịch</span></div><div class="col-lg-8"><span class="eyebrow">{{ $doctor->specialty->name }}</span><h1 class="section-title mt-2">{{ $doctor->academic_title }} {{ $doctor->user->name }}</h1><p class="lead-copy">{{ $doctor->biography }}</p><div class="row g-3 my-4"><div class="col-sm-4"><small class="text-muted">Kinh nghiệm</small><strong class="d-block">{{ $doctor->years_of_experience }} năm</strong></div><div class="col-sm-4"><small class="text-muted">Phòng khám</small><strong class="d-block">{{ $doctor->room_number }}</strong></div><div class="col-sm-4"><small class="text-muted">Phí khám</small><strong class="d-block">{{ number_format($doctor->consultation_fee) }}đ</strong></div></div><h2 class="h6 fw-bold">Lịch gần nhất</h2><div class="d-flex flex-wrap gap-2">@forelse($schedules as $schedule)<span class="btn btn-soft">{{ $schedule->work_date->format('d/m') }} · {{ substr($schedule->start_time,0,5) }}–{{ substr($schedule->end_time,0,5) }}</span>@empty<span class="text-muted">Lịch đang được cập nhật</span>@endforelse</div><a class="btn btn-primary mt-4" href="{{ auth()->check() && auth()->user()->patient ? route('patient.appointments.create') : route('login') }}">Đặt lịch với bác sĩ</a></div></div></div></div></section>@endsection
