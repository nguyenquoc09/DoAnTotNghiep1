@extends('layouts.guest') @section('title','Đội ngũ bác sĩ') @section('content')<section class="section section-soft"><div class="container"><div class="row align-items-end mb-4"><div class="col-lg-6"><span class="eyebrow">Đội ngũ chuyên gia</span><h1 class="section-title mt-2">Tìm bác sĩ phù hợp</h1></div><div class="col-lg-6"><form class="d-flex gap-2"><input class="form-control" name="q" value="{{ request('q') }}" placeholder="Tên bác sĩ..."><select class="form-select" name="specialty"><option value="">Mọi chuyên khoa</option>@foreach($specialties as $s)<option value="{{ $s->id }}" @if(request('specialty')==$s->id) selected @endif>{{ $s->name }}</option>@endforeach</select><button class="btn btn-primary">Tìm</button></form></div></div><div class="row g-4">@forelse($doctors as $doctor)<div class="col-md-6 col-lg-4"><a href="{{ route('doctors.show',$doctor) }}" class="medical-card d-block text-reset"><div class="doctor-avatar mb-3"><i class="bi bi-person-heart"></i></div><small class="text-primary fw-bold">{{ $doctor->specialty->name }}</small><h2 class="h5 fw-bold mt-1">{{ $doctor->academic_title }} {{ $doctor->user->name }}</h2><p class="text-muted">{{ Str::limit($doctor->biography,110) }}</p><div class="d-flex justify-content-between"><span>{{ $doctor->years_of_experience }} năm KN</span><strong>{{ number_format($doctor->consultation_fee) }}đ</strong></div></a></div>@empty<div class="empty-state"><i class="bi bi-search"></i><p>Chưa tìm thấy bác sĩ phù hợp.</p></div>@endforelse</div><div class="mt-4">{{ $doctors->links() }}</div></div></section>@endsection
