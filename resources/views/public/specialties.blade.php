@extends('layouts.guest') @section('title','Chuyên khoa') @section('content')<section class="section section-soft"><div class="container"><span class="eyebrow">Dịch vụ chuyên môn</span><h1 class="section-title mt-2 mb-4">Chuyên khoa tại An Tâm</h1><div class="row g-4">@foreach($specialties as $specialty)<div class="col-md-6 col-lg-4"><a href="{{ route('doctors.index',['specialty'=>$specialty->id]) }}" class="medical-card d-block text-reset"><div class="icon-box"><i class="bi bi-heart-pulse"></i></div><h2 class="h5 fw-bold mt-3">{{ $specialty->name }}</h2><p class="text-muted">{{ $specialty->description }}</p><span class="text-primary fw-semibold">{{ $specialty->doctors_count }} bác sĩ</span></a></div>@endforeach</div><div class="mt-4">{{ $specialties->links() }}</div></div></section>@endsection
