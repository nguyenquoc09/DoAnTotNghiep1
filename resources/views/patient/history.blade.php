@extends('layouts.patient') @section('title','Lịch sử khám') @section('pageTitle','Lịch sử sức khỏe') @section('content')<div class="panel"><div class="panel-title"><div><h2>Hồ sơ khám bệnh</h2><small class="text-muted">Các kết quả được sắp xếp từ mới nhất.</small></div></div>@forelse($records as $record)<a href="{{ route('patient.records.show',$record) }}" class="d-flex gap-3 align-items-center border-bottom py-3 text-reset"><div class="icon-box"><i class="bi bi-journal-medical"></i></div><div class="flex-grow-1"><strong>{{ $record->diagnosis ?: 'Phiên khám '.$record->record_code }}</strong><small class="d-block text-muted">{{ $record->created_at->format('d/m/Y') }} · BS. {{ $record->doctor->user->name }}</small></div><i class="bi bi-chevron-right text-muted"></i></a>@empty<div class="empty-state"><i class="bi bi-journal-medical"></i><p>Chưa có hồ sơ khám bệnh.</p></div>@endforelse<div class="mt-3">{{ $records->links() }}</div></div>@endsection
