@extends('layouts.patient') @section('title','Đặt lịch khám') @section('pageTitle','Đặt lịch khám mới') @section('content')<div class="row justify-content-center"><div class="col-xl-9"><div class="panel"><div class="stepper"><div class="step active">1. Bác sĩ</div><div class="step active">2. Ngày & giờ</div><div class="step active">3. Lý do khám</div><div class="step active">4. Xác nhận</div></div><h2 class="h4 fw-bold">Chọn thời gian phù hợp với bạn</h2><p class="text-muted">Các khung giờ hiển thị trong 14 ngày tới và được cập nhật theo thời gian thực.</p><form method="post" action="{{ route('patient.appointments.store') }}" class="row g-3">@csrf<input type="hidden" name="patient_id" value="{{ auth()->user()->patient->id }}"><div class="col-md-6"><label class="form-label">Lịch bác sĩ *</label><select class="form-select" name="doctor_schedule_id" id="patientSchedule" required><option value="">Chọn bác sĩ và ngày</option>@foreach($schedules as $s)<option value="{{ $s->id }}" data-doctor="{{ $s->doctor_id }}" data-slots='@json($s->available_slots)'>{{ $s->work_date->format('d/m/Y') }} · {{ $s->doctor->user->name }} · {{ $s->shift_name }}</option>@endforeach</select></div><input type="hidden" name="doctor_id" id="patientDoctor"><div class="col-md-3"><label class="form-label">Khung giờ *</label><select class="form-select" name="appointment_time" id="patientSlot" required><option value="">Chọn lịch trước</option></select></div><div class="col-md-3"><label class="form-label">Dịch vụ</label><select class="form-select" name="medical_service_id"><option value="">Khám theo phí bác sĩ</option>@foreach($services as $s)<option value="{{ $s->id }}">{{ $s->name }} · {{ number_format($s->price) }}đ</option>@endforeach</select></div><div class="col-12"><label class="form-label">Bạn muốn bác sĩ hỗ trợ điều gì? *</label><textarea class="form-control" rows="3" name="reason" placeholder="Ví dụ: Đau đầu kéo dài 3 ngày..." required>{{ old('reason') }}</textarea></div><div class="col-12"><label class="form-label">Triệu chứng bổ sung</label><textarea class="form-control" rows="3" name="symptoms_note" placeholder="Thông tin này giúp bác sĩ chuẩn bị tốt hơn.">{{ old('symptoms_note') }}</textarea></div><div class="col-12"><div class="alert alert-info"><i class="bi bi-info-circle"></i> Lịch trực tuyến sẽ ở trạng thái chờ xác nhận. Bạn có thể hủy trước khi phòng khám tiếp nhận.</div><button class="btn btn-primary btn-lg">Xác nhận đặt lịch</button></div></form></div></div></div>@push('scripts')<script>var s=document.getElementById('patientSchedule'),t=document.getElementById('patientSlot'),d=document.getElementById('patientDoctor');s.addEventListener('change',function(){var o=s.options[s.selectedIndex];d.value=o.dataset.doctor||'';var slots=JSON.parse(o.dataset.slots||'[]');t.innerHTML=slots.length?slots.map(function(x){return '<option value="'+x+'">'+x+'</option>'}).join(''):'<option value="">Ca đã đầy</option>';});</script>@endpush @endsection
