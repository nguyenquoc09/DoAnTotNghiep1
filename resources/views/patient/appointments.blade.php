@extends('layouts.patient') @section('title','Lịch hẹn của tôi') @section('pageTitle','Lịch hẹn của tôi') @section('content')<div class="panel"><div class="panel-title"><h2>Hành trình thăm khám</h2><a class="btn btn-primary btn-sm" href="{{ route('patient.appointments.create') }}">Đặt lịch mới</a></div><div class="table-responsive"><table class="table"><thead><tr><th>Thời gian</th><th>Bác sĩ</th><th>Lý do khám</th><th>Trạng thái</th><th></th></tr></thead><tbody>@forelse($appointments as $a)<tr><td><strong>{{ $a->appointment_date->format('d/m/Y') }}</strong><small class="d-block text-muted">{{ substr($a->appointment_time,0,5) }} · {{ $a->appointment_code }}</small></td><td>{{ $a->doctor->user->name }}<small class="d-block text-muted">{{ $a->specialty->name }}</small></td><td>{{ Str::limit($a->reason,70) }}</td><td><span class="badge-status {{ $a->status }}">{{ $a->status }}</span></td><td>@if(in_array($a->status,['pending','confirmed']))<details><summary class="btn btn-sm btn-outline-danger">Hủy lịch</summary><form method="post" action="{{ route('patient.appointments.cancel',$a) }}" class="mt-2">@csrf<textarea class="form-control mb-2" name="reason" placeholder="Lý do hủy" required></textarea><button class="btn btn-danger btn-sm">Xác nhận hủy</button></form></details>@endif</td></tr>@empty<tr><td colspan="5" class="empty-state"><i class="bi bi-calendar-heart"></i><p>Bạn chưa có lịch hẹn nào.</p></td></tr>@endforelse</tbody></table></div>{{ $appointments->links() }}</div>@endsection
