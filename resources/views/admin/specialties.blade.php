@extends('layouts.admin') @section('title','Chuyên khoa') @section('pageTitle','Quản lý chuyên khoa') @section('content')<div class="row g-4"><div class="col-lg-4"><div class="panel"><h2 class="h5 fw-bold">Thêm chuyên khoa</h2><form method="post" action="{{ route('admin.specialties.store') }}">@csrf<label class="form-label mt-3">Tên chuyên khoa</label><input class="form-control" name="name" required><label class="form-label mt-3">Mô tả</label><textarea class="form-control" name="description" rows="4"></textarea><label class="form-label mt-3">Trạng thái</label><select class="form-select" name="status"><option value="active">Hoạt động</option><option value="inactive">Tạm ẩn</option></select><button class="btn btn-primary w-100 mt-4">Thêm chuyên khoa</button></form></div></div><div class="col-lg-8"><div class="panel"><div class="table-responsive"><table class="table"><thead><tr><th>Chuyên khoa</th><th>Bác sĩ</th><th>Trạng thái</th><th></th></tr></thead><tbody>@foreach($items as $item)<tr><td><strong>{{ $item->name }}</strong><small class="d-block text-muted">{{ Str::limit($item->description,70) }}</small></td><td>{{ $item->doctors_count }}</td><td><span class="badge-status {{ $item->status }}">{{ $item->status }}</span></td><td><details><summary class="btn btn-sm btn-soft">Sửa</summary><form class="mt-2" method="post" action="{{ route('admin.specialties.update',$item) }}">@csrf @method('PUT')<input class="form-control mb-2" name="name" value="{{ $item->name }}"><textarea class="form-control mb-2" name="description">{{ $item->description }}</textarea><select class="form-select mb-2" name="status"><option value="active">Hoạt động</option><option value="inactive" @if($item->status==='inactive') selected @endif>Tạm ẩn</option></select><button class="btn btn-primary btn-sm">Lưu</button></form></details></td></tr>@endforeach</tbody></table></div>{{ $items->links() }}</div></div></div>@endsection
